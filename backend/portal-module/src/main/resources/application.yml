spring:
  application:
    name: customer-service-portal

  datasource:
    url: jdbc:postgresql://localhost:5432/customer_service
    username: postgres
    password: postgres123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081
  servlet:
    context-path: /

app:
  jwt:
    secret: customer-service-secret-key-for-jwt-token-generation-and-validation-2024-2025
    expiration: 86400000 # 24 hours in milliseconds

